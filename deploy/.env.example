# ==============================================================================
# SENTINEL PLATFORM - ENVIRONMENT CONFIGURATION
# ==============================================================================
# 1. Copy this file to .env
# 2. Fill in the values below.
# 3. DO NOT commit the real .env file to version control.
# ==============================================================================

# --- CRYPTOGRAPHY ---
# The Master Key Encryption Key (KEK).
# REQUIRED: Must be a 32-byte (256-bit) hex string (64 characters).
# Generate with: openssl rand -hex 32
SENTINEL_MASTER_KEY=

# --- INFRASTRUCTURE ---
# The name used for container prefixes and logging.
PROJECT_NAME=Sentinel

# --- DATABASE (MongoDB) ---
# Connection string for the Core service.
# For Docker Compose, use the service name 'mongo'.
# Format: mongodb://user:pass@host:port
MONGO_URI=mongodb://admin:securepassword@mongo:27017

# Root credentials for initializing the Mongo container.
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=securepassword
# Optional: The specific database name to use (Default: sentinel_db)
# MONGO_DB_NAME=sentinel_db

# --- DATABASE GUI (Mongo Express) ---
# Credentials for the web-based admin panel (http://localhost:8081).
ME_CONFIG_MONGODB_ADMINUSERNAME=admin
ME_CONFIG_MONGODB_ADMINPASSWORD=securepassword
# Connection string for the GUI to find the DB.
ME_CONFIG_MONGODB_URL=mongodb://admin:securepassword@mongo:27017/

# --- OBJECT STORAGE (MinIO / S3) ---
# Access keys for the MinIO container (or AWS S3).
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
# The endpoint URL. For Docker Compose, use 'http://minio:9000'.
# If using AWS S3, use 'https://s3.amazonaws.com'.
S3_ENDPOINT=http://minio:9000

# --- AV SCANNER SERVICE ---
# A shared secret key to authenticate requests between Core and Scanner.
# Generate with: openssl rand -hex 16
SCANNER_API_KEY=change-me-to-something-secure
# The internal URL of the scanner service.
SCANNER_URL=http://sentinel-scanner:5000

# --- PRIVACY ENGINE (Microsoft Presidio) ---
# URLs for the PII detection and anonymization services.
# These match the container names/ports in docker-compose.yml.
PRESIDIO_ANALYZER_URL=http://presidio-analyzer:3000
PRESIDIO_ANONYMIZER_URL=http://presidio-anonymizer:3000